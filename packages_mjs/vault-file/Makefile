.PHONY: test run clean install

# Run tests
test:
	npm test

# Run module (requires ts-node or similar)
run:
	npx tsx src/core.ts

# Install dependencies
install:
	npm install

# Clean node_modules
clean:
	rm -rf node_modules
